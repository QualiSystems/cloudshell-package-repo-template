name: deploy package
on:
  push:
    paths-ignore:
      - README.md
jobs:
  tox-ci:
    uses: ./.github/workflows/reusing-tox-ci-py37-39.yml
  pypi-deploy:
    needs: tox-ci
    uses: ./.github/workflows/reusing-deploy-pypi.yml
    secrets: inherit
